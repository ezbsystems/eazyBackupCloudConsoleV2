<html>
<head>
  <title>Initializing Recovery Environment</title>
  <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
  <hta:application
    id="E3RecoveryLoader"
    border="thin"
    caption="yes"
    showintaskbar="yes"
    singleinstance="yes"
    sysmenu="yes"
    windowstate="normal"
  />
  <style type="text/css">
    body {
      font-family: Segoe UI, Arial, sans-serif;
      background: #0b1220;
      color: #e2e8f0;
      margin: 0;
      padding: 20px;
    }
    .card {
      background: #111827;
      border: 1px solid #1f2937;
      border-radius: 12px;
      padding: 18px;
      max-width: 560px;
      margin: 0 auto;
    }
    h2 {
      margin: 0 0 10px 0;
      font-size: 20px;
    }
    p {
      margin: 0 0 10px 0;
      color: #cbd5e1;
      line-height: 1.45;
    }
    .row {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-top: 12px;
    }
    .spinner {
      width: 20px;
      height: 20px;
      border: 3px solid #1e3a8a;
      border-top-color: #60a5fa;
      border-right-color: #60a5fa;
      border-radius: 50%;
      animation: spin 0.9s linear infinite;
      flex: 0 0 20px;
    }
    .status {
      font-size: 14px;
      color: #dbeafe;
    }
    .hint {
      margin-top: 12px;
      color: #94a3b8;
      font-size: 12px;
    }
    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>Initializing Recovery Environment</h2>
    <p>eazyBackup is loading WinPE services, storage, and network drivers.</p>
    <div class="row">
      <div class="spinner"></div>
      <div id="statusText" class="status">Starting initialization...</div>
    </div>
    <div class="hint">This can take a few minutes on systems with large driver packs.</div>
  </div>
  <script type="text/javascript">
    (function () {
      var shell = new ActiveXObject("WScript.Shell");
      var fso = new ActiveXObject("Scripting.FileSystemObject");
      var flagPath = shell.ExpandEnvironmentStrings("%SystemRoot%\\System32\\e3-loader-ready.flag");
      var dots = 0;

      function updateStatus() {
        dots = (dots + 1) % 4;
        var suffix = "";
        for (var i = 0; i < dots; i++) suffix += ".";
        document.getElementById("statusText").innerText = "Preparing recovery console" + suffix;
      }

      setInterval(function () {
        try {
          if (fso.FileExists(flagPath)) {
            window.close();
            return;
          }
        } catch (e) {
        }
        updateStatus();
      }, 700);
    })();
  </script>
</body>
</html>
